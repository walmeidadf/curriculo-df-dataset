# Currículo em Movimento DF – Dataset Pipeline
# Instale com: pip install -r requirements.txt
# Após a primeira instalação, fixe as versões exatas com: pip freeze > requirements.lock

# Extração de PDF para Markdown
docling>=2.5.0

# LLM para enriquecimento (Groq API – Llama 3.3 70B)
groq>=0.9.0

# Variáveis de ambiente (.env)
python-dotenv>=1.0.0

# Publicação e download do dataset (HuggingFace)
datasets>=2.19.0
huggingface_hub>=0.23.0

# Validação do schema JSON
jsonschema>=4.22.0

# Manipulação de dados e exportação Parquet
pandas>=2.2.0
pyarrow>=15.0.0

# Barra de progresso
tqdm>=4.66.0

# Logging estruturado
loguru>=0.7.0
